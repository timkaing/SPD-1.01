  def duplicateZero(self, input: List[int]) -> None:
        """
         Do not return anything, modify arr in-place instead.
         """

          result = []
           for x in input:
                result.append(x)
                if x == 0:
                    res.append(x)
            for i in range(len(input)):
                input[i] = result[i]
